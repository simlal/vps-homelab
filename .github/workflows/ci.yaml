name: CI

on:
  push:
    branches: ["main"]
  pull_request:
    branches: ["main"]
  workflow_dispatch:

jobs:
  build-deploy:
    runs-on: [self-hosted, linux]

    steps:
      - uses: actions/checkout@v4
        with:
          path: infra

      - name: Sync repo to persistent infra dir (preserve data and .env)
        run: |
          rsync -a --delete \
            --exclude '.env' \
            --exclude 'kuma-data' \
            --exclude 'caddy_data' \
            --exclude 'caddy_config' \
            --exclude 'site' \
            infra/ /home/simlal/infra/

      - name: Deploy infra
        working-directory: /home/simlal/infra
        run: docker compose -p infra up -d

      - name: Healthcheck caddy
        working-directory: /home/simlal/infra
        run: docker inspect --format='{{.State.Health.Status}}' caddy
